services:
  vault:
    build: .
    ports:
      - "4000:4000"
      - "5173:5173"
    volumes:
      - ./data:/app/data
    environment:
      PORT: "4000"
      NODE_ENV: development
      NODE_OPTIONS: --experimental-sqlite
